"use client";
/**
 * PLACEHOLDER — lo implementa el módulo de Presupuestos.
 * Debe mostrar un banner cuando algún presupuesto del mes actual supera su umbral
 * (alert_threshold_pct) o el 100%. Se monta en AppShell en todas las pantallas.
 */
export function BudgetAlertBanner() {
  return null;
}
